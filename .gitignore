node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.nonsecret

# SSH keys — never commit private keys
sachmuelbeck
sachmuelbeck.pub

# Secrets directory — never commit actual keys
# Use secrets/TEMPLATE/ instead
secrets/*.txt
!secrets/.gitkeep

# Runtime SQLite databases (generated at runtime; not part of the source tree)
*.db
*.db-wal
*.db-shm

# Admin-generated VIP passes (contains real access codes — never commit)
vip_passes.json

# Python cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# IDE
.idea/

# Claude Code internal directory (settings, memory, worktrees)
.claude/

# Captured output artifacts
.output.txt

# Private business strategy & expansion plans
Seireitei/MARKETS_EXPANSION.md

# Personal notes and TODO files (if you create them)
NOTES.md
TODO.md
PRIVATE.md

# Any local testing or scratch files
scratch/
temp/
test-data/
